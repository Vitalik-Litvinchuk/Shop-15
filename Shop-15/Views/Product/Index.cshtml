@model IEnumerable<Shop_15.Models.Product>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-success">Product List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-action="Upsert" class="btn btn-primary">
                Create new product
            </a>
        </div>
    </div>
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Product name</th>
                    <th>Category type</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td width="30%">@item.Category.Name</td>
                        <td width="30%">@item.Price</td>
                        <td width="20%" class="text-center">
                            <div class="btn-group w-75">
                                <a class="btn btn-warning mr-1" asp-route-Id="@item.Id" asp-action="Upsert">Edit</a>
                                <a class="btn btn-danger ml-1" asp-route-Id="@item.Id"  asp-action="Delete">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>
            No category found.
        </p>
    }
</div>